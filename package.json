{
  "name": "mission-control-v2",
  "private": true,
  "workspaces": ["server", "web"],
  "scripts": {
    "dev": "cd server && bun run dev & cd web && bun run dev",
    "build": "cd web && bun run build && cd ../server && bun run build",
    "migrate": "bun run server/scripts/migrate.ts",
    "seed": "bun run server/scripts/seed.ts"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6"
  }
}
